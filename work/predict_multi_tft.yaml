model_path: multi_tft.ckpt
train_kwargs: train_multi_tft.yaml
verbose: true
past: 3600
predict_kwargs:
  return_index: true
  mode: prediction  # prediction or quantiles
engine_kwargs:
  url: sqlite:///multi_tft@prediction.db
write_kwargs:
  name: data
  if_exists: append