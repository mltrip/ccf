defaults:
 - ./@train_kwargs: train_multi_tgt_tft
 - query@data_kwargs.query: predictions_multi_tgt_tft_naive

model_path: ~
verbose: true
past: 60
predict_kwargs:
  return_index: true
  mode: prediction  # prediction or quantiles
data_kwargs: {}