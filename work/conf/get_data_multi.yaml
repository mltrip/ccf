defaults:
 - feeds: all
 - markets: three
 - streams: 1000_d5_t
 - run_kwargs: none

executor_kwargs:
  max_workers: 7
market_kwargs:
  verbose: false
feeds_kwargs:
  split: 1
  delay: 3
  before: 3600
  verbose: false