defaults:
 - ./@train_kwargs: train_mlp

model_path: mlp.ckpt
verbose: true
past: 3600
predict_kwargs:
  return_index: true
  mode: prediction  # prediction or quantiles
engine_kwargs:
  url: sqlite:///mlp@prediction.db
write_kwargs:
  name: data
  if_exists: append