defaults:
 - ./@train_kwargs: train_mid_tft_lograt_min
 - query@data_kwargs.query: predictions_mid_tft_lograt_min_naive
 
model_path: NaiveZero
verbose: true
past: 62
delay: 1
predict_kwargs:
  return_index: true
  mode: prediction  # prediction or quantiles
data_kwargs: {}