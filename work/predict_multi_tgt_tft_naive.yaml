model_path: ~
train_kwargs: train_multi_tgt_tft.yaml
verbose: true
past: 3600
predict_kwargs:
  return_index: true
  mode: prediction  # prediction or quantiles
engine_kwargs:
  url: sqlite:///multi_tgt_tft_naive@prediction.db
write_kwargs:
  name: data
  if_exists: append